---
title: "Project 2 - Dataset 3"
author: "Keith Colella"
date: "`r Sys.Date()`"
output: html_document
knit: (function(input, ...) {
    rmarkdown::render(
      input,
      output_dir = "../D607/output"
    )
  })
---

```{r, message = FALSE}
library(tidyverse)
library(reshape2)
```

## Read in CSV

TBD

```{r}
data <- read_csv('https://github.com/kac624/cuny/raw/main/D607/data/healthcare_empl.csv',
                 skip = 3)

head(data)

tail(data)
```


